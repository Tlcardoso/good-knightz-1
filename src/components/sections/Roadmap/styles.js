import styled from 'styled-components'

export const Section = styled.section`
  min-height: 100vh;
  margin: 50px auto;
  
  h1 {
    margin-bottom: 35px;
  }
  
  .topInfoWrapper{
    max-width: 650px;
    margin: 0 auto 130px auto;
  }

  .roadmapCardsWrapper {
    > div {
      margin: 40px auto;
    }
  }
`
